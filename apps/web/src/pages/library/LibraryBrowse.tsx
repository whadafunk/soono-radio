// Phase 4 fills this in. Stub for now so the route resolves and the tab works.
export function LibraryBrowse() {
  return (
    <div className="bg-zinc-900 border border-zinc-800 rounded-lg p-8 text-center text-zinc-400">
      <p className="text-lg">Library browse coming in Phase 4.</p>
      <p className="text-sm mt-2">For now, head to <span className="text-indigo-400">Upload</span> to add audio.</p>
    </div>
  );
}
